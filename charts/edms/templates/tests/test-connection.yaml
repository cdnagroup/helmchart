apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edms.fullname" . }}-test-connection"
  labels:
    {{- include "edms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
